import React from 'react'

const StartPage = () => {
    return (
        <div>
            <button type="text">Start</button>
            <button type="text">Instruction</button>
        </div>
    )
}

export default StartPage
